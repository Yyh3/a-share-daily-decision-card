# 分析层补齐方案 — 从「数据层 70%」到「完全体」

> 创建时间：2026-09-01 15:40（UTC+8）
> 触发：`HANDOFF-缺口补齐.md` 第 8 节「暂缓项」共 9 项 —— 事件卡全字段、主线判定、盘中节奏叙事、三情景剧本、次日验证清单回溯、龙虎榜席位级解码、国家队持仓、PE 估值分位、宏观事件日历
> 结论前置：**9 项里 5 项其实是可自动化的数据层，2 项半自动，仅 2 项真正不可得**。上一版 HANDOFF 里「PE 分位拿不到」「龙虎榜属分析层」两条判断是错的，本轮实测已推翻。

---

## 0. 本轮实测：数据源可得性矩阵

本轮重新下载了参考卡原文（142KB，10 个章节 301 行正文），逐章拆解字段后对每一项做了实盘探测。结果如下。

| # | 暂缓项 | 参考卡章节 | 实测结论 | 数据源 | 请求数/日 |
|---|---|---|---|---|---|
| 1 | PE 估值分位 | 02 盘面全景 | ✅ **可做**（此前误判为不可得） | 中证官网 `index-perf` 带 `peg` 字段 | 1（40 年区间一次拿全） |
| 2 | 龙虎榜席位级解码 | 03 杠杆与国家队 | ✅ **可做** | 东财 datacenter `RPT_BILLBOARD_DAILYDETAILSBUY` | 1（支持按日过滤+排序） |
| 3 | 盘中节奏时间轴 | 02 盘面全景 | ✅ **可做** | 腾讯 `ifzq.gtimg.cn` mkline 分钟线 | 3（上证/创业板/科创50） |
| 4 | 解禁/中报收官排雷 | 09 风险排雷 | ✅ **可做** | 东财 datacenter `RPT_LIFT_STAGE` | 1 |
| 5 | 龙虎榜个股净买/净卖 | 03 + 09 | ✅ **可做** | 东财 datacenter `RPT_DAILYBILLBOARD_DETAILSNEW` | 1 |
| 6 | 国家队/ETF 动向 | 03 | ⚠️ **半可做** | push2delay ETF clist（份额需另找报表名） | 1 |
| 7 | 事件卡全字段 | 04 | ⚠️ **半自动**（数据可喂，叙事要 LLM） | 已采集的资金流/涨停梯队/龙虎榜 → LLM 生成 | 0（LLM 侧） |
| 8 | 主线判定 / 情景剧本 / 验证清单 | 05 / 09 | ⚠️ **半自动**（纯 LLM，需证据包+校验） | 同上 | 0 |
| 9 | 宏观事件日历 | 07 | ⚠️ **半可做**（无 API，但可静态维护） | 年度静态 JSON（PMI/非农/CPI/LPR 日期固定） | 0 |
| — | 美债 10Y 收益率 | 08 | ❌ **确认不可得** | 东财/新浪/腾讯全部候选 secid 返空 | — |
| — | 汇金系持仓 | 03 | ❌ **不可得** | 需公募中报+Wind 统计，无免费源 | — |

### 0.1 推翻的两个旧结论（重要）

**①「PE 估值分位免费源拿不到」→ 错。**

中证官网 `index-perf` 接口返回的 `peg` 字段就是市盈率，且 `startDate/endDate` 支持任意区间。实测取中证全指（000985）近 10 年：

```
GET https://www.csindex.com.cn/csindex-home/perf/index-perf
    ?indexCode=000985&startDate=20160831&endDate=20260831
→ 2428 个交易日 PE 序列；当前 PE 18.27，近 2428 日分位 65.3%
```

参考卡写的是「中证全指 PE 21.2，10 年 87% 分位」——口径完全一致（只是时点不同）。**1 次请求，且这个接口项目已经在用（沪市成交额就是它）**。

**②「龙虎榜席位级解码属分析层，本期不做」→ 错在归类。**

东财 datacenter 有三个可用报表，全都在 `datacenter-web` 域名（不在 push2 限流范围）：

| 报表名 | 内容 | 实测样本 |
|---|---|---|
| `RPT_DAILYBILLBOARD_DETAILSNEW` | 个股级：净买额、成交额、上榜原因、次日/5 日涨幅 | 8-31 共 17 页（~1700 条） |
| `RPT_BILLBOARD_DAILYDETAILSBUY` | **席位级**：营业部名称、买入/卖出/净额、3 日上涨概率 | 8-31 共 210 页 |
| `RPT_OPERATEDEPT_TRADE_DETAILS` | 营业部 × 个股交易明细 | 722k 页（按日过滤后可用） |

实测返回的 `OPERATEDEPT_NAME` 直接就是参考卡里点名的席位：

```
"国盛证券股份有限公司宁波桑田路证券营业部"  ← 参考卡原文：「知名游资席位」
  SECURITY_CODE=002396（星网锐捷） NET=+2.32亿  RISE_PROBABILITY_3DAY=66.7%
```

**这意味着参考卡 03 章「席位级传导（营业部明细）」整张表都能自动重建**，包括「机构专用」「沪股通专用/深股通专用」这些特殊席位。

### 0.2 新发现：盘中节奏是数据层，不是叙事层

参考卡 02 章的「盘中节奏时间轴」看起来像人工写的快讯，但底层就是分钟线：

```
GET https://ifzq.gtimg.cn/appstock/app/kline/mkline?param=sh000001,m30,,20
→ m30: [["202609011500","3982.14","3979.89","3985.00","3977.00","66172425.00",{},"13.65"], ...]
   [时间, 开, 收, 高, 低, 成交量, {}, 换手]
```

⚠️ 踩坑记录：域名必须是 `ifzq.gtimg.cn`，**不是** `web.ifzq.gtimg.cn`（后者 mkline 路径 DNS 解析失败，但日线 fqkline 路径正常——同一个域名下不同 path 的 DNS 行为不一致，很怪但实测如此）。

有了分钟线，「09:56 创业板涨超 1%」「13:42 创业板跌超 1%」这类节奏可以**规则化生成**（扫描分钟线找极值点 + 拐点时刻），不必依赖财联社快讯。

---

## 1. 核心架构决策：证据包 → LLM 填空 → 校验渲染

9 项里真正需要 LLM 的只有第 7、8 项（事件卡叙事、主线判定、情景剧本）。**不要让 LLM 直接产出页面或自由文本**，否则会失去可复现性和可回溯性——这正是本项目「确定性规则」立身之本。

分层：

```
collect_data.py（联网）
    ├─ 行情/资金/广度/天梯/两融/全球      ← 已完成
    ├─ 新增：龙虎榜 / 解禁 / PE分位 / 分钟线 / ETF
    └─ 额外产出 data/raw/brief-YYYY-MM-DD.md   ← 证据包（人类可读 + LLM 可读）
                ↓
LLM 层（人工或脚本调用）
    读证据包 → 只填 schema 中指定的数组字段（events / mainline / scenarios / verification）
    → 写回 data/raw/eod-YYYY-MM-DD.json 的对应键
                ↓
build_data.py（离线）
    ├─ validate_layer()：必填字段 / 枚举值 / 数值范围 / 日期一致性 校验
    ├─ 校验失败 → 该章节降级为空 + 风险栏标注「分析层未生成」
    └─ 渲染（确定性）
                ↓
app.js → index.html
```

**关键约束**：

1. LLM 只写 `events` / `mainline` / `scenarios` / `verification` / `intraday_narrative` 五个键，其余全部由采集器与 build 脚本生成；
2. LLM 输出必须过 `validate_layer()`，不合格就降级留空——**宁可空着也不显示未校验的推断**；
3. 证据包 `brief-*.md` 是 LLM 的唯一输入，里面不含任何模型输出，保证每天的输入可复现；
4. 所有 LLM 生成内容在前端带 `ai-generated` 标记 + 来源字段。

---

## 2. 逐项实现路径

### 2.1 PE 估值分位（数据层，1 请求）

**采集**

```python
CSINDEX_PE = [("000985", "中证全指"), ("000300", "沪深300"), ("000922", "中证红利")]
# 一次取 10 年（约 2430 个交易日），本地缓存，此后只增量追加
def fetch_index_pe(code, start, end) -> dict[str, float]:
    url = ("https://www.csindex.com.cn/csindex-home/perf/index-perf?"
           f"indexCode={code}&startDate={start}&endDate={end}")
    return {r["tradeDate"]: float(r["peg"]) for r in get_json(url)["data"] if r.get("peg")}

def pe_percentile(series: dict[str, float], years: int = 10) -> dict:
    """纯函数：当前 PE 在近 N 年序列中的百分位。"""
    items = sorted(series.items())[-years * 244:]
    values = [v for _, v in items]
    cur = values[-1]
    return {"pe": round(cur, 2), "percentile": round(sum(1 for v in values if v <= cur) / len(values) * 100, 1),
            "window_years": years, "samples": len(values)}
```

**schema**

```json
"valuation": [
  {"name": "中证全指", "pe": 18.27, "percentile": 65.3, "window": "10年", "samples": 2428},
  {"name": "沪深300", "pe": 14.25, "percentile": 58.1, "window": "10年", "samples": 2428}
]
```

**展示**：并入现有「01 盘面全景」表格右侧，加两列 `PE` / `10年分位`；分位 >80% 标红、<20% 标绿，并注明「口径=中证官网 index-perf peg 字段，与申万/Choice 口径不同」。

> 这一步直接补掉了参考卡 02 章与 05 章「估值 87 分位」的论据缺口，也是主线判定「为什么没有主线」的解释项之一。

### 2.2 龙虎榜（数据层，2 请求）

**采集**（按日期过滤，避免全量）

```python
LHB_STOCK = "RPT_DAILYBILLBOARD_DETAILSNEW"      # 个股级
LHB_SEAT  = "RPT_BILLBOARD_DAILYDETAILSBUY"      # 席位级（买/卖/净 + 营业部名）

# 个股级：只取净买 TOP 10 / 净卖 TOP 10
# 席位级：只取 |NET| TOP 20 的营业部记录
```

**关键字段映射**（实测值 → 参考卡展示）

| 参考卡字段 | 接口字段 | 备注 |
|---|---|---|
| 净买（席位入场） | `NET` > 0 聚合到板块 | 需 stock→sector 映射 |
| 净卖（席位兑现） | `NET` < 0 | 同上 |
| 营业部名称 | `OPERATEDEPT_NAME` | 「机构专用」「沪股通专用」是特殊值，单独归类 |
| 上榜原因 | `EXPLANATION` | 「日涨幅达到15%的前5只证券」等 |
| 席位性质判读 | ⚠️ 需 LLM | 「疑似机构定制分仓而非典型游资」是推断 |
| 传导解读 | ⚠️ 需 LLM | 「龙头净卖+跟风净买=分歧扩散」是推断 |

**schema**

```json
"dragon_tiger": {
  "date": "2026-08-31",
  "stock_top": [  {"code":"002396","name":"星网锐捷","net":3.01,"deal":9.40,"reason":"连续三日涨幅偏离20%","change":10.00} ],
  "stock_bottom": [],
  "seats": [ {"dept":"国盛证券宁波桑田路","code":"002396","name":"星网锐捷","net":2.32,
              "buy":2.37,"sell":0.05,"reason":"...","rise_prob_3d":66.7} ],
  "seat_kinds": {"institutional": [...], "northbound": [...], "known_hotmoney": [...]},
  "note": "净买额口径（东财 datacenter）；席位性质判读为规则+LLM 混合，未确认项标注「待确认」"
}
```

**展示**：新增「03B 龙虎榜与席位解码」章节，两张表：

- 个股表：代码 / 名称 / 净买额 / 成交额 / 上榜原因 / 当日涨幅，按净买降序；
- 席位表：营业部 / 标的 / 净额 / 买 / 卖 / 上榜原因，按 |净额| 降序；`机构专用`、`沪股通专用/深股通专用` 打不同底色标签（这是规则，不需要 LLM）。

### 2.3 盘中节奏（数据层，3 请求）

**采集**：`ifzq.gtimg.cn` mkline，m30 粒度取 3 个指数（上证/创业板/科创50），当日 8 根。

**规则化生成节奏**（纯函数，可单测）

```python
def intraday_rhythm(bars: list[list], base_close: float) -> list[dict]:
    """分钟线 → 节奏事件。纯函数。

    规则：
      1. 每根 bar 算 pct = close / base_close - 1
      2. 标记日内最高/最低点所在时刻
      3. 标记首次触及 ±1% 的时刻、最后一次回落的时刻
      4. 尾盘 30 分钟方向（收官方向）
    """
```

输出示例：

```json
"intraday": [
  {"time": "10:00", "pct": 1.05, "tag": "首次涨超1%", "index": "创业板指"},
  {"time": "11:30", "pct": 0.42, "tag": "早盘见顶回落"},
  {"time": "13:30", "pct": -1.12, "tag": "首次跌超1%", "index": "创业板指"},
  {"time": "14:30", "pct": -1.85, "tag": "日内低点"},
  {"time": "15:00", "pct": -1.41, "tag": "尾盘回抽"}
]
```

**展示**：现有「02 盘面全景」下方加一条迷你时间轴（SVG 折线 + 关键点标注），比参考卡的纯文本快讯更直观且完全客观。

> 参考卡那句「尾盘深中华A多次炸板回封7板」来自个股分时，属于个股级，**本期不做**（需要额外 88 次请求，性价比低）；只做指数级节奏。

### 2.4 解禁与风险排雷（数据层，1 请求）

```python
# RPT_LIFT_STAGE 支持 filter=(FREE_DATE>=...)
# 字段：FREE_DATE 解禁日、LIFT_MARKET_CAP 解禁市值(万元)、FREE_SHARES_TYPE 股份类型
```

**展示**：并入「10 来源与风险提示」的「风险排雷」子块，列出未来 7 日解禁家数与合计市值，**只列市值 TOP 5 的个股**（避免吓人又不实用）。规则化提示：`解禁市值/流通市值 > 10%` 标红。

### 2.5 ETF / 国家队（半可做，1 请求）

能拿：push2delay ETF clist（`fs=b:MK0021,b:MK0022,b:MK0023,b:MK0024`，1588 只），可算「宽基 ETF vs 科技 ETF 当日平均涨幅」作为国家队动向的**弱代理**。

拿不到：ETF 份额变化（`RPT_ETF_HIS_SHARE` 报表不存在，需另找）、汇金持仓（Wind 中报统计）。

**建议**：只做「宽基 vs 主题 ETF 涨幅对比」一条规则化指标，明确标注「这是价格代理，不是持仓数据」，不编造持仓结论。

### 2.6 事件卡全字段（LLM 层）

参考卡 04 章每张事件卡 8 个字段，数据层能喂 4 个，LLM 补 4 个：

| 字段 | 来源 | 自动度 |
|---|---|---|
| 事件（发生了什么） | ✅ 证据包：涨停梯队 + 资金流 + 龙虎榜 | 半自动（LLM 归纳） |
| 产业传导 | ⚠️ LLM | 人工/LLM |
| 客观分析 | ⚠️ LLM | 人工/LLM |
| 评分（事件影响 4 维 + 市场验证 4 维） | ⚠️ LLM 打分，build 校验范围 | 半自动 |
| 个股映射（位置/关联度/读数） | ✅ 涨停池 + 龙虎榜可算出位置与读数 | 自动 |
| 下一信号 | ⚠️ LLM | 人工/LLM |
| 风险与反证 | ⚠️ LLM | 人工/LLM |
| **反向声音** | ⚠️ LLM（最需要人的地方） | 人工/LLM |

**评分校验规则**（build_data 内，防 LLM 乱填）：

```python
def validate_event(ev: dict) -> list[str]:
    errs = []
    for dim in ("conf", "industry", "growth", "persist"):   # 事件影响 4 维
        if not isinstance(ev["impact"].get(dim), int) or not 1 <= ev["impact"][dim] <= 5:
            errs.append(f"impact.{dim} 必须是 1-5 整数")
    for dim in ("breadth", "ladder", "volume", "carry"):    # 市场验证 4 维
        if not isinstance(ev["verify"].get(dim), int) or not 1 <= ev["verify"][dim] <= 5:
            errs.append(f"verify.{dim} 必须是 1-5 整数")
    if ev["impact_total"] != sum(ev["impact"].values()) or not 0 <= ev["impact_total"] <= 20:
        errs.append("impact_total 必须等于四项之和且 ≤20")
    if ev.get("direction") not in ("正向", "负向", "中性"):
        errs.append("direction 枚举错误")
    return errs
```

**展示**：现有「06 热点事件卡」章节升级——每张卡从现在的 4 字段扩到 8 字段，加「评分条（事件影响 x/20 ｜ 市场验证 x/20）」和「个股映射表」；反向声音用不同的底色（浅黄）单独成块，视觉上和「客观分析」区分开。

### 2.7 主线判定 / 三情景 / 验证清单（LLM 层 + 回溯机制）

这三个是参考卡的「决策闭环」，也是最难自动化的部分，但**框架可以半自动**：

**主线判定**——参考卡用 5 个判据，其中 4 个可自动计算：

| 判据 | 参考卡原文 | 自动化 |
|---|---|---|
| 板块宽度 | 上涨行业 60-80% | ✅ 从 clist 全量板块算 |
| 轮动胜率 | 昨日 6 大领涨方向：1 持续 / 2 半 / 3 灭 | ✅ 需缓存昨日领涨板块，次日比对 |
| 资金集中度 | 最大流出 -122.6 亿 > 最大流入 +22.5 亿 | ✅ 直接算 |
| 涨停归类 | 基础化工 16 只 > 计算机 8 只 | ✅ 涨停池 hybk 聚合 |
| 流动性 | 2.10 万亿 = 120 日均量 80% | ✅ 需成交额缓存（已有） |
| **结论句** | 「不存在产业级单一主线」 | ⚠️ LLM |

即：**5 个判据全部可算，只有最后一句结论需要 LLM**。让 LLM 基于已算好的 5 个数出一句结论，出错面就很小。

**三情景剧本**——参考卡格式固定（悲观/中性/乐观 + 概率 + 触发条件 + 应对）。可让 LLM 填 `probability`（校验和=100）、`trigger`、`action`。

**验证清单回溯**（这是最有价值、也最容易被忽略的一块）：

```
data/verify_log.json
{
  "2026-08-31": {
    "issued": [
      {"id": 1, "text": "9:30 官方制造业PMI（前值49.2）", "then": "<49 → 政策预期升温；≥50 → 顺周期修复"},
      {"id": 2, "text": "贵金属板块低开幅度与承接", "then": "低开<2%回拉 → 恐慌未扩散"}
    ],
    "checked": null
  },
  "2026-09-01": {
    "issued": [...],
    "checked": {"1": {"result": "✓", "actual": "PMI 49.8，未跌破49", "note": "..."}}
  }
}
```

**可自动回溯的项**（用数据判定，不需要人）：

| 验证点类型 | 自动判据 | 数据来源 |
|---|---|---|
| 成交额守 X 万亿 | `turnover >= X` | 已有 |
| 涨停家数 ≥ N | `limit_up >= N` | 已有 |
| 5 日资金是否延续 | `day5 符号不变` | 已有 |
| 板块是否涨停 ≥3 家 | `zt_sector[sector] >= 3` | 已有 |
| 某股是否断板 | `code not in today_zt_codes` | 已有（zt_codes 缓存） |
| 指数是否站上 X 点 | `close >= X` | 已有 |

**不能自动回溯的**（需人工）：PMI 数值、政策消息、监管函。这些标 `△`（待人工）。

**展示**：新增「09B 上期验证回溯」章节，三列表格：验证点 / 预判 / 实际（✓成功 / ✗失败 / △待人工），底部一行「本期命中率 x/y」。**这块是让卡片从「日报」升级为「可问责的决策系统」的关键**，建议优先级仅次于龙虎榜。

### 2.8 宏观事件日历（半可做）

实测：金十 API 502、财联社 404、东财 4 个候选报表名全不存在。

**务实的做法**：维护 `data/static/macro_calendar.json`，一年写一次。PMI（每月最后一天/1 号）、非农（每月第一个周五）、CPI/PPI（9-10 号）、LPR（20 号）、FOMC（公布日程）日期规律固定，人工维护成本低且**比抓来的数据更可靠**。

```json
[
  {"date": "2026-09-01", "time": "09:30", "event": "财新中国制造业PMI", "prev": "50.9",
   "bull": "回升 → 顺周期修复", "bear": "走弱 → 政策预期升温", "region": "CN", "importance": 3},
  {"date": "2026-09-04", "event": "美国8月非农", "prev": "7月疲软+下修7.9万",
   "bull": "弱 → 加息定价回摆，贵金属急修", "bear": "强 → 加息坐实，金银/成长双杀",
   "region": "US", "importance": 5}
]
```

**展示**：新增「07 未来事件日历」章节，未来 14 日时间轴，⭐ 标重要性，每条带「利好/利空映射」两栏。这是从参考卡 07 章直接照搬的结构，且**完全数据驱动、零推断**。

### 2.9 确认不可得的两项

- **美债 10Y 收益率**：东财 `100.US10Y`/`100.UST10Y`/`100.US10YR` 全部返空，新浪 `globalbd_us10y`/`gb_$tnx`/`gb_$tyx`/`hf_ZN` 全部返空，腾讯 `usTNX` 无匹配。**继续保留「不列示」，不做估算**。若将来要补，唯一可行路径是人工每天填一个数字进 `data/static/`（美债一天一个数，人工成本可接受）。
- **汇金系持仓**：Wind 中报统计口径，无免费源。用 2.5 的 ETF 价格代理替代，并明确标注是代理。

---

## 3. 前端章节重排（补齐后）

| # | 章节 | 状态 | 数据来源 |
|---|---|---|---|
| 00 | 今日核心裁决 | 已有 | 规则 |
| 01 | 盘面全景与风格坐标（+ PE 分位列） | 扩建 | 腾讯 + 中证 |
| 02 | 近五日市场 | 已有 | 腾讯 |
| 02B | **盘中节奏时间轴** | 新增 | 腾讯分钟线 |
| 03 | 资金流向与持续性 | 已有 | 东财 |
| 03B | **龙虎榜与席位解码** | 新增 | 东财 datacenter |
| 04 | **涨停梯队与情绪结构** | 已有 | push2ex |
| 05 | **热点事件卡（全字段 8 项）** | 升级 | LLM + 证据包 |
| 06 | **主线判定（5 判据自动 + 结论 LLM）** | 新增 | 混合 |
| 07 | **未来事件日历** | 新增 | 静态 JSON |
| 08 | 全球市场联动 | 已有 | push2delay + 新浪 |
| 09 | 市场广度与两融 | 已有 | 新浪 + 东财 |
| 09B | **上期验证回溯** | 新增 | verify_log + 规则 |
| 10 | 蓄力观察池 / 次日情景预案 | 已有 / 升级 | 规则 + LLM |
| 11 | 来源与风险提示（+ 解禁排雷） | 扩建 | 东财 |

---

## 4. 分阶段路线图

按「性价比 = 数据价值 / 实现成本」排序，每阶段独立可交付、可回滚。

### P1（建议先做，纯数据层，2-3 小时）

1. **PE 估值分位**（1 请求，推翻旧结论，直接补上参考卡的核心论据）
2. **龙虎榜个股 + 席位**（2 请求，解锁参考卡 03 章整章）
3. **解禁排雷**（1 请求，补风险排雷最后一环）

> 这三块全是数据层，可单测、可降级、无 LLM 依赖，做完卡片覆盖率从 ~70% 到 ~85%。

### P2（半自动，需要接 LLM，半天）

4. **证据包 `brief-*.md` 生成**（把已有数据渲染成人+LLM 都能读的 Markdown）
5. **事件卡全字段**（LLM 填空 + `validate_event()` 校验）
6. **验证清单回溯机制**（`verify_log.json` + 自动判定规则，可自动回溯的项先上）

### P3（框架自动化 + 少量 LLM，半天）

7. **主线判定**（5 判据自动算，只有结论句要 LLM）
8. **宏观事件日历**（静态 JSON，一年维护一次）
9. **盘中节奏时间轴**（分钟线 + 规则化极值检测）

### P4（可选）

10. ETF 价格代理（弱信号，标注清楚）
11. 三情景剧本（LLM 全填）

---

## 5. 需要用户决策的三件事

1. **LLM 接入方式**：走脚本调用（需要在仓库里放 API key / 环境变量）？还是人工把证据包喂给对话模型、再把返回的 JSON 贴回 `eod-*.json`？后者零成本零依赖，但每天要手动一次。**建议先做后者（P2 阶段）**，跑顺了再考虑脚本化。

2. **美债 10Y**：确认放弃，还是每天人工填一个数进 `data/static/us10y.json`？（参考卡 08 章「2Y 4.356% / 10Y 4.722% 跳升」是它论证「Warsh 鹰派 → 金银重挫」的关键一环，缺了它对全球章节的说服力有损。）

3. **龙虎榜数据量**：8-31 单日席位明细有 210 页（约 2 万条）。建议只取 `|NET|` TOP 20 + 特殊席位（机构专用/沪股通/深股通）全部，约 5 次请求；是否接受？还是只要 TOP 20（1 次请求）？

---

## 6. 与上一版 HANDOFF 的修订对照

| 上一版结论 | 本轮修订 |
|---|---|
| 「PE 分位免费源拿不到，明确放弃」 | ❌ 错。中证官网 `index-perf` 的 `peg` 字段，1 次请求拿 10 年序列 |
| 「龙虎榜席位级解码属分析层，本期不做」 | ❌ 归类错误。席位数据是纯数据层（`RPT_BILLBOARD_DAILYDETAILSBUY`），只有「性质判读」是分析层 |
| 「国家队持仓数据层做不了」 | ⚠️ 部分对。持仓确实拿不到，但 ETF 价格代理可做（需标注是代理） |
| 「盘中节奏叙事属分析层」 | ⚠️ 部分对。指数级节奏可从分钟线规则化生成（数据层）；个股分时节奏才是分析层 |
| 「宏观事件日历需人工」 | ✅ 对，但可静态维护（日期规律固定，年维护一次） |
| 「美债无免费源」 | ✅ 确认，本轮又试了 5 个新候选，全部返空 |
