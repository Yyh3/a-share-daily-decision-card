# 决策卡缺口补齐 — 任务 Handoff

> 创建时间：2026-08-31 03:40（UTC+8）
> 任务状态：**缺口盘点完成，升级方案已定，尚未动工**
> 用户决策（2026-08-31）：**四块全做——涨停梯队、指数多窗口+风格坐标、两融、全球市场**；事件卡等分析层暂不做

---

## 1. 背景

对照 WorkBuddy 参考卡（2026-08-28 版，"完全体 v1.0"，十个章节 930 行文本，本地存档
`workbuddy_ref.html` 120KB，位于临时工作区；GitHub 仓库无此文件，需要时重新从原链接下载：
`https://workbuddy-space-static.codebuddy.work/page/JB4JF3f3Wu6Ofxdle4w9rp/0/decision-card-2026-08-28.html`），
当前卡片只覆盖约 30%：核心裁决（无触发/失效条件）、五日市场表、广度、资金流+持续性+蓄力池、来源披露。

用户已确认补齐以下四块**数据层**内容（全部可用免费接口自动采集，无需编数）：

1. **涨停梯队与情绪结构**（参考卡第 06 章）
2. **指数多窗口 + 风格坐标**（参考卡第 01/02 章的一部分）
3. **两融余额**（参考卡第 03 章"杠杆与国家队"）
4. **全球市场联动**（参考卡第 08 章）

明确**不做**（属于 LLM/人工分析层）：事件卡全字段、主线判定、盘中节奏叙事、情景剧本、验证清单回溯。

---

## 2. 任务一：涨停梯队与情绪结构（优先级最高）

### 数据源（零新增请求）

涨停池 push2ex 接口**已经在采集**（`fetch_pool("zt", ...)`），池内每只股票的 `lbc` 字段就是连板数，
目前只用来数了涨停家数，字段被浪费了。DT 池同理。

### 需要计算并输出的内容

```json
"limit_ladder": {
  "date": "2026-08-31",
  "ladder": [
    {"board": 7, "stock": "深中华A", "name": "黄金", "note": "多次炸板天量"},
    {"board": 6, "stock": "万向德农", "name": "粮食", "note": "9天6板 缩量晋级"},
    {"board": 5, "stock": "海鸥住工", "name": "…", "note": "封单力度9.65%全场第一"}
  ],
  "metrics": {
    "limit_up": 88, "limit_down": 11,
    "zha_ban": null,        // 炸板家数：push2ex 有 getTopicZBPool（炸板池），需加一个请求
    "seal_rate": null,      // 封板率 = 涨停 / (涨停+炸板)
    "promotion_rate": null  // 连板晋级率：需要上一交易日的涨停名单做对比（缓存里存每日涨停股代码列表即可）
  }
}
```

### 实现要点

- `collect_data.py`：ZT 池解析时把 `lbc`、`n`（股票名）、`hybk`（行业）、`zbc`（炸板次数）一起
  提取；每日涨停股代码列表写入 `daily_stats.json` 缓存（当日 vs 前日列表求交集 → 晋级率）；
- 新增 `fetch_pool("zb", yyyymmdd)` 调炸板池 getTopicZBPool（push2ex 域名，不受限流影响，
  请求数 +1/日）；
- `build_data.py`：排序生成天梯（按 board 降序，同板按封单/成交额），计算晋级率/封板率；
- `index.html` + `app.js`：新增"06 涨停梯队"章节（表格+情绪指标卡）。
- 参考口径差异：参考卡封板率 84% vs 证券时报 77.36%，两口径已在参考卡内标注——我们也应
  写明口径（封板率=收盘涨停/(收盘涨停+炸板)）。

## 3. 任务二：指数多窗口 + 风格坐标

### 数据源（腾讯 fqkline，已有函数，只加代码）

参考卡用 7 个指数。腾讯代码：

| 指数 | 代码 | 用途 |
|---|---|---|
| 上证指数 | sh000001 | 已有 |
| 深证成指 | sz399001 | 新增（参考卡全景表有） |
| 创业板指 | sz399006 | 已有 |
| 科创50 | sh000688 | 已有 |
| 沪深300 | sh000300 | 新增（风格坐标） |
| 中证1000 | sh000852 | 新增（风格坐标，小盘代理） |
| 中证红利 | sh000922 | 新增（风格坐标，红利/价值代理） |

### 需要计算并输出

```json
"index_panel": [
  {"name": "上证指数", "close": 3952.18, "pct": -0.11, "ret5": 1.20, "ret20": 3.13, "ret60": -2.60}
],
"style_note": "小盘主导：中证1000 20日+8.9% vs 沪深300 +0.5%（规则生成，非叙事）"
```

### 实现要点

- `TENCENT_INDEXES` 列表扩到 7 项；`lmt` 从 8 提到 65（60 日窗口需 61 根K线）——请求仍是 7 次；
- 5/20/60 日区间收益纯计算（`close / close[-N] - 1`）；
- 风格坐标用**规则化**表达（中证1000 20日 - 沪深300 20日 之差，正=小盘占优负=大盘占优），
  不做叙事分析；PE 分位（参考卡"中证全指PE 21.2，10年87%分位"）免费源拿不到，**明确放弃并
  在风险栏注明**，不要编数；
- `build_data.py` 输出 `index_panel` + `style_note`；前端加"02 盘面全景"表格列。
- 注意：新增指数只用于涨跌幅和区间收益，成交额仍只用上证+深证综指口径（中证官网+深交所官网），
  避免口径混用。

## 4. 任务三：两融余额

### 数据源（沪深交易所官网，权威 T+1）

- 深交所：`https://www.szse.cn/api/report/ShowReport/data?SHOWTYPE=JSON&CATALOGID=1837_xxpl&TABKEY=tab1&TABKEYExtra=&txtQueryDate=YYYY-MM-DD`
  （融资融券交易，单日查询，结构与 1803 相同，已验证过 szse 域名可访问）
- 上交所：SSE 官网 query.sse.com.cn 两融接口（此前测试该域返回空，**优先做深交所，沪市可从
  东财 datacenter-web 拿**：`https://datacenter-web.eastmoney.com/api/data/v1/get?reportName=RPTA_WEB_RZRQ_GGMX`——
  注意 datacenter-web 未被封但报表名需要实测确认；实在不行先只做深市+全市场合计降级方案）

### 需要计算并输出

```json
"margin": {
  "balance": 26567.87,          // 全市场两融余额（亿元）
  "change": 118.94,             // 较前日变动
  "as_of": "2026-08-28",        // T+1 披露，注意标注数据日期≠市场日期
  "note": "T+1披露，8.31数据次一交易日可得"
}
```

### 实现要点

- 两融是 T+1 披露：8-31 的卡片只能带 8-28 的两融数，schema 里必须有 `as_of` 字段并在前端
  明示（参考卡也这样标注："两融=21财经8-28晨报（T+1披露8.27数）"）；
- 前日余额可从缓存取差值（`daily_stats.json` 加 `margin_balance` 字段）；
- **明确放弃**：参考卡还有"国家队/汇金持仓"（来自公募中报+ETF份额公示，属人工汇编），
  数据层做不了，不做。龙虎榜席位级解码同样属东财 datacenter 接口+分析层，本期不做。

## 5. 任务四：全球市场联动

### 数据源（腾讯/新浪全球接口，全部免费）

- 腾讯 fqkline 全球指数：代码 `usDJI`（道指）`usSPX`（标普）`usIXIC`（纳指）`usSOX`（费半）、
  `jpNI225`（日经）`krKOSPI`、`twTWSE`（台湾加权）`hkHSI`（恒指）`hkHSTECH`（恒生科技）；
  同一 `web.ifzq.gtimg.cn` fqkline 接口，加代码即可（**先实测代码有效性**，腾讯代码有坑）；
- 美债/金银/美元/LME：新浪 `hq.sinajs.cn/list=…`（gb_10y_usa 之类）或腾讯 `qt.gtimg.cn`；
  具体代码需实测——**这是四块里唯一需要先探测的**，预留一次探测环节（约 10 个代码试错）。

### 需要计算并输出

```json
"global_markets": [
  {"name": "道指", "close": 53560, "pct": -0.02, "note": "周内节奏（可选）"},
  {"name": "费半", "close": null, "pct": -3.47, "note": "A股算力链映射核心"},
  {"name": "现货金", "close": 4453.67, "pct": -2.95},
  ...
]
```

### 实现要点

- 时区对齐问题：美股收盘是北京时间次日 04:00，A 股 8-31 卡片应带"8-28 美股收盘"（周五），
  采集时取最新已收盘数据即可，`as_of` 字段必须写清是哪个市场的哪个交易日；
- 全球行情为**软依赖**（拿不到就整块留空+标注，不影响主卡）；
- 参考卡第 08 章的"资金流窜主线/传导矩阵"是分析层，不做，只做行情总表。

## 6. 统一实现规范（四块共用）

1. **schema 演进**：新字段（`limit_ladder`、`index_panel`、`margin`、`global_markets`）加到
   `data/raw/eod-*.json` 顶层；`sample.json` 同步补演示示例；README schema 一节更新；
2. **软硬依赖分级**：涨停梯队（数据已在手，硬依赖）、指数多窗口（腾讯，硬依赖）、两融（软依赖，
   T+1）、全球市场（软依赖）——软依赖失败照常出快照+标注；
3. **测试先行**：每块的纯计算逻辑（晋级率、区间收益、天梯排序）写进 `scripts/test_logic.py`，
   先写测试再实现；
4. **请求预算**：新增请求预计 +10~12 次/日（炸板池 1、腾讯指数 7、全球行情 ~10），
   全部走腾讯/push2ex/深交所/datacenter-web，**不碰 push2/push2his**（仍可能处于封禁）；
5. **前端**：每块一个新章节（按参考卡编号排 06 涨停梯队、08 全球联动；指数全景并入现有 01/02 章）；
   `app.js` 更新后记得同步改 `index.html` 里 `app.js?v=` 版本参数（**踩过的坑**：不改会缓存旧
   脚本渲染 NaN）；
6. 完成后：HANDOFF.md 加 0.4 节、README 请求预算表更新、SKILL.md（仓库+本地技能库两处）同步、
   commit + push、单测全过、浏览器实测渲染、更新 memory。

## 7. 验收清单

- [ ] 涨停梯队：天梯至少显示到 3 板层，晋级率/封板率有值且有口径说明
- [ ] 指数全景：7 指数 × (收盘/当日/5日/20日/60日) 表格完整
- [ ] 风格坐标：中证1000-沪深300 20日差值及规则化解读
- [ ] 两融：余额+变动+as_of 日期标注（T+1 说明可见）
- [ ] 全球市场：美股三大+费半+日韩台港+金银+美债（LME 可缺）总表
- [ ] 四块软依赖降级路径各自实测（人为断 URL 验证）
- [ ] `test_logic.py` 全过；请求统计打印无 push2/push2his
- [ ] 页面渲染实测（含打印预览）；GitHub 已推送

## 8. 暂缓项（明确不做，留给后续）

事件卡全字段（评分/个股映射/反向声音）、主线判定、盘中节奏叙事、三情景剧本、次日验证清单回溯、
龙虎榜席位级解码、国家队持仓、PE 估值分位、宏观事件日历——全部属于 LLM/人工分析层或数据不可得，
待数据层四块稳定后再评估（可用"采集器输出结构化摘要喂给 LLM"的方式补分析层）。

---

**接手即从这里开始：先做任务一（涨停梯队），数据已在手，纯计算+前端，最快见效。**
